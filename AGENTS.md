# DeskBox development workflow

## Handoff bắt buộc — fork `bbkien2312/DeskBox-mrKBB`

Phần này tóm tắt các fix chính của fork ngày 19/08/2026 và phần hoàn thiện Fork 4 ngày 20/08/2026. Commit ngày 18/08 chỉ là baseline source ban đầu. Khi sửa lỗi mới, giữ nguyên các hành vi dưới đây trừ khi người dùng duyệt thay đổi khác.

### Nguồn, phiên bản và phát hành

- Cây `main` của fork là nguồn sự thật. `deskbox_original` chỉ là baseline để đối chiếu; không dùng lại source cũ để ghi đè cây này.
- Bản phát hành online gần nhất: `1.4.2.1-fork.9` (tag `v1.4.2.1-fork.9`, source `ad4480c`, manifest `be8ed31`). Fork 10 hiện chỉ là thay đổi/build local; chỉ nâng `DeskBoxForkBuildNumber`, không đổi upstream version `1.4.2.1`.
- Installer Fork 4 đã build tại `Output\DeskBox_Setup_1.4.2.1_x64.exe` và đã publish ở GitHub Release tag `v1.4.2.1-fork.4`. `release/stable.json` online đã trỏ Fork 4, asset, dung lượng và SHA-256 đã được đối chiếu. Fork 3 phải so sánh `forkBuildNumber=3` với manifest `4` để hiện update.
- Luồng updater nằm ở `scripts\publish-fork-update.ps1`, `Services\AppUpdateService.cs`, `Services\AppBuildMetadata.cs` và `Models\AppUpdateManifest.cs`. Giữ protocol version riêng của fork, `forkBuildNumber` tách khỏi version upstream và tên tag `v1.4.2.1-fork.N`.
- Script phát hành phải tạo SHA-256/manifest UTF-8 không BOM. Không giả định PowerShell có `Get-FileHash`; script đã có fallback BCL.
- **Installer Fork 7:** setup online vẫn framework-dependent và nhỏ; bắt buộc kiểm tra/cài đủ Visual C++ 2015-2022 Redistributable, .NET 10 Runtime và Windows App Runtime 2.2 đúng kiến trúc. `SetupLogging=yes` giúp lấy log trong Temp hoặc `/LOG="..."` khi cần. Nếu Windows App Runtime vừa cài system-wide nhưng process Setup không elevated chưa nhìn thấy package, yêu cầu restart rồi chạy lại, không báo lỗi verification vĩnh viễn. `scripts\prepare-offline-prerequisites.ps1` và `installer\DeskBox.Prerequisites.iss` dựng gói rescue offline; binary vendor chỉ ở `artifacts\prerequisites` (gitignored), được upload làm Release asset qua `-BuildOfflinePrerequisites`, không commit vào Git và không để updater tự tải.
- **Phát hành Fork 7 (28/08/2026):** source `b54b895`, manifest `dbb3b06`, tag `v1.4.2.1-fork.7`; setup online 25.396.088 bytes, SHA-256 `68FE0EA7DD2C57DB0A32AB450378D859F19BFCF4B4BA0CBB14E98E43C13C5BB6`. Release còn có `DeskBox_Prerequisites_1.4.2.1_x64.exe` 165.279.618 bytes và SHA riêng. Đã tải ngược manifest/asset/checksum từ GitHub: build 7, URL, size và SHA đều khớp. `release/stable.json` chỉ trỏ asset setup online; trước khi kết luận updater hoạt động phải lặp lại đối chiếu này cho mọi Fork mới.
- **Phát hành Fork 8 (28/08/2026):** source build `906610c`, manifest cuối `695793b`, tag [v1.4.2.1-fork.8](https://github.com/bbkien2312/DeskBox-mrKBB/releases/tag/v1.4.2.1-fork.8). Setup online `DeskBox_Setup_1.4.2.1_x64.exe` có 25.398.825 bytes, SHA-256 `66208975EC78986B8EF1EA39B4DBD8A30572A030DC793F572544E0B50958AAF6`; GitHub Release trả đúng size/digest đó. Manifest Contents API trên nhánh `main` đã xác nhận build 8, URL và `forkCommit=906610c`; raw CDN có thể trả cache manifest cũ trong ít phút sau push, nên không tạo release/asset mới chỉ vì lệch cache tức thời. Release giữ thêm setup prerequisites offline x64 từ Fork 7; updater chỉ tải setup online.
- **Phát hành Fork 9 (29/08/2026):** source build `ad4480c`, manifest commit `be8ed31`, tag [v1.4.2.1-fork.9](https://github.com/bbkien2312/DeskBox-mrKBB/releases/tag/v1.4.2.1-fork.9). Setup online `DeskBox_Setup_1.4.2.1_x64.exe` có 25.702.451 bytes, SHA-256 `BF81928B4C959DFBE855DB399E362FECEEF8CA49729C5141CD4A08FBD7B9E461`. Đã đọc ngược GitHub Release API và Contents API: tag, hai asset installer/SHA, build 9, `forkCommit`, dung lượng, SHA và download URL đều khớp. `release/stable.json` đã online nên updater Fork 8 có thể nhận Fork 9.
- Khi hoàn thành bất kỳ hạng mục nào phải cập nhật cả `..\PROJECT.md` và phần handoff này bằng tiếng Việt: phạm vi, file đã đổi, lỗi/nguyên nhân/cách xử lý, test, số đo hiệu năng nếu có, commit và thông tin release. Đây là điều kiện trước khi push/phát hành.
- Checklist release bắt buộc: tăng riêng `DeskBoxForkBuildNumber`, giữ upstream version `1.4.2.1`, build/test/kiểm tra mở installer, tạo manifest UTF-8 không BOM và SHA, push source, publish tag/asset/release, đối chiếu `release/stable.json` online với asset, test cập nhật từ Fork N-1. Không thay bằng chỉ `git push`.

### Desktop organizer — hành vi phải giữ

- `vi-VN` là locale đầy đủ; khi đổi ngôn ngữ phải refresh cả cửa sổ tổ chức. File chính: `Strings\vi-VN.json`, `Services\LocalizationService.cs`, `Views\DesktopOrganizationWindow.xaml.cs`.
- Rule tổ chức nhận file, Folder, shortcut/app, system shortcut, media, archive và document. Folder có hai phần không được gộp nhầm: rule `Folders` chọn box đích, còn watcher tự nhận folder mới trực tiếp trên Desktop.
- Scanner có thể quét Desktop cá nhân, Public Desktop và nội dung nằm trong box khi người dùng bật tùy chọn. File/folder thay đổi sau scan phải bị bỏ qua riêng với log, không làm hủy kế hoạch của các mục khác.
- Watcher phải debounce và chờ file/folder ổn định trước khi move. Các module trọng tâm: `DesktopAutoOrganizationWatcher`, `DesktopOrganizationScanner`, `DesktopOrganizationClassifier`, `DesktopOrganizationPlanner`, `DesktopOrganizationTransaction` và `DesktopOrganizationLogService`.
- Thông báo auto-organize được gộp theo khoảng ngắn, không báo từng file. Giữ `DesktopOrganizationNotificationCoalescer` và log mức `INFO/OK/WARNING/ERROR` có timestamp.
- Với organize cùng volume, `DesktopOrganizationTransaction` gọi transfer plan với `useShellProgress:false` để dùng `File.Move` metadata nhanh. Khác volume vẫn phải rơi về copy/delete có rollback trong `FileService`; không tự giới hạn hoặc bỏ qua chỉ vì khác ổ.
- Box tạo mới bởi Desktop organizer mặc định `WidgetSortMode.Type`; box cũ phải giữ sort mode người dùng đã chọn.

### Tương tác file và vòng đời app

- Trong box: type-ahead theo ký tự đầu tên giống Explorer; double-click theo khoảng Windows mở mục/đổi tên. Các sửa liên quan chủ yếu ở `FileSurfaceContent`, `WidgetViewModel` và `Win32Helper`.
- Shortcut `.lnk` kéo từ box Apps and shortcuts sang Explorer phải dùng payload/OLE native để Explorer quyết định Move/Copy, không tạo bản copy rơi về Desktop. Giữ `FileItemDragPackage`, `NativeFileDragSource` và `VirtualShortcutDragProvider` fallback.
- Khi người dùng chọn thoát hoàn toàn, watcher dừng trước rồi các box managed được trả về Desktop, có undo và journal `managed-storage-restore-journal.json`. Không restore khi shutdown vì update/restart. Lần khởi động sau, tùy chọn resync phải đưa các mục đã trả về lại box theo cấu hình.
- Một EXE DeskBox thứ hai thường tự thoát vì single-instance mutex và signal app đang chạy; không kết luận là crash khi test publish nếu bản cài đặt còn mở.

### Hiệu năng và chụp màn hình

- Thumbnail ảnh dùng cache disk demand-driven tại `%LOCALAPPDATA%\DeskBox\cache\thumbnails`; video tiếp tục dùng Windows thumbnail provider. Không thêm quét cache lúc startup ngoài tạo thư mục/cleanup nhỏ.
- Cache icon, bitmap và thumbnail RAM đã được giảm giới hạn. Số liệu disk thumbnail cache được ghi trong `PerformanceLogger`; đừng tăng cache lại mà không đo RAM thực tế.
- `Ctrl+Alt+S` mở `ScreenshotCaptureWindow`: chụp ảnh màn hình trước khi overlay hiện, rê để chọn cửa sổ, click để khóa, sau đó Sao chép/Lưu. Dùng DWM extended frame bounds nếu có, fallback `GetWindowRect`.
- **Fork 5 (20/08/2026):** baseline Fork 4 có 6 box/khoảng 105 item, private 258–318 MB và working set 363–439 MB sau idle; cache disk đúng nhưng chỉ 18 PNG/0,16 MB nên không phải nguyên nhân RAM. `WidgetViewModel` trước đó hydrate/retry toàn bộ icon. Fork 5 giới hạn hydrate lúc đầu 12 icon/box, `FileItemSurface` yêu cầu icon khi item được WinUI hiện thực hóa và nhả icon khi item ra khỏi viewport 1 giây; vẫn giữ cache disk/global LRU. Rủi ro cần test UI: kiểm tra icon xuất hiện khi cuộn và không bị nhấp nháy khi đổi layout.
- **Fork 6 (20/08/2026):** Fork 5 gây placeholder ở box lớn, đặc biệt Apps and shortcuts, vì giới hạn 12 icon và vì WinUI có thể báo `Unloaded` khi vẫn tái sử dụng container. Sửa bằng initial budget 48 icon/box và tuyệt đối không đặt `WidgetItem.Icon = null` theo `Unloaded`; giữ lazy nạp bổ sung và các giới hạn global cache. Cache `%LOCALAPPDATA%\DeskBox\cache\thumbnails` chỉ chứa thumbnail của ảnh (không chứa icon shortcut/document), nên số file cache không phản ánh số icon trong Apps. Test `1788/1788` passed; cần update Fork 6 và kiểm tra box Apps/Images/Tài Liệu.
- **Fork 5 screenshot:** click nhanh khóa cửa sổ/toàn màn hình, nhấn-kéo tạo vùng tự do; snapshot luôn tạo trước overlay. Tọa độ vùng được quy đổi giữa XAML DIP và pixel capture để đúng ở DPI khác 100%. Copy/Lưu ghi log `window`, `monitor` hoặc `region`; Esc hủy, Chọn lại reset.
- **Screenshot Ctrl+C (Fork 8):** khi vùng/cửa sổ/toàn màn hình đã được khóa trong `ScreenshotCaptureWindow`, `Ctrl+C` gọi đúng `ExportSelectionAsync(copyToClipboard: true)` như nút `Sao chép (Ctrl+C)`. Không đăng ký hotkey toàn hệ thống, không chạy khi đang kéo vùng hoặc đang xuất ảnh; vì vậy không ghi đè Clipboard ngoài overlay và không tạo thêm bitmap/cache RAM. Lỗi Clipboard tiếp tục đi qua xử lý lỗi/log sẵn có, overlay được giữ lại để thử lại. `publish-fork-update.ps1` nhận thêm `VietnameseReleaseNotes` và `EnglishReleaseNotes` để manifest của các fork sau không bị dùng nhầm ghi chú phát hành cũ.
- **Fork 9 — Chụp cửa sổ không bị che:** `ScreenshotCaptureWindow` chỉ chuyển nhánh **cửa sổ đã chọn** sang `SelectedWindowScreenshotService`. Service lấy `GraphicsCaptureItem` trực tiếp theo HWND, dùng `Windows Graphics Capture` + D3D11 để mã hoá PNG, nên không còn cắt từ ảnh Desktop đóng băng có thể bị cửa sổ khác phủ lên. Vùng kéo tự do, Desktop và toàn màn hình vẫn gọi `CropFrozenSnapshot` y hệt Fork 8. D3D/frame pool chỉ tồn tại trong lúc bấm Sao chép/Lưu và được dispose; log thành công là `window-isolated engine=wgc`. Nếu Windows/driver/cửa sổ không hỗ trợ WGC, log `window-wgc-fallback` và fallback `PrintWindow`; nếu fallback cũng thất bại thì UI giữ overlay để thử lại. Package `Vortice.Direct3D11` chỉ phục vụ nhánh bấm chụp này. Đã build Debug x64 và test `1788/1788`; cần kiểm tra thủ công một cửa sổ bị che sau update. Không dừng DeskBox bản cài đang chạy chỉ để test Debug vì nó có thể khôi phục file managed về Desktop. Installer online x64 đã build từ source `ad4480c` tại `Output\DeskBox_Setup_1.4.2.1_x64.exe`, 25.702.451 bytes, SHA-256 `BF81928B4C959DFBE855DB399E362FECEEF8CA49729C5141CD4A08FBD7B9E461`; GitHub Release `v1.4.2.1-fork.9` và `release/stable.json` online đã được đối chiếu nên updater Fork 8 có thể thấy bản này.
- **Hotfix phát hành Fork 8:** `publish-fork-update.ps1` phải gọi `git -C $RepositoryRoot rev-parse HEAD`, không dùng thư mục PowerShell hiện hành. Nếu không, gọi script qua đường dẫn tuyệt đối từ `DeskBox-dev` sẽ làm `forkCommit` rỗng và dừng trước khi build/upload. Đây là một phần của quy trình phát hành, cần giữ cho các fork sau.
- **Release tách bước:** khi installer được build trước rồi gọi script với `-SkipBuild`, truyền `-ForkCommit <commit mã đã build>`. Nếu để script tự đọc `HEAD` sau một manifest commit trung gian, manifest sẽ ghi sai commit nguồn dù installer/SHA vẫn đúng. `ForkCommit` được thêm từ Fork 8 để khóa đúng provenance của asset.
- Build Debug x64 và bộ test cô lập `1788/1788` đã đạt sau thay đổi Fork 5. Installer Release `Output\DeskBox_Setup_1.4.2.1_x64.exe` đã tạo (25.396.661 bytes, SHA-256 `913D35B19C9A1C89C032B5D664F635F5BC877D8DCFF9D49483C9C8F51F106C28`) và Release/manifest online đã khớp. Cảnh báo compiler cũ (nullability/AOT/binding) không phát sinh từ Fork 5; GUI crop/scroll cần người dùng xác nhận trên desktop thật sau update vì không dừng bản cài đang chạy để tránh restore 105 file giữa phiên.

### 29/08/2026 — Fork 10 đang kiểm thử: snapshot Desktop và x86

- Lịch sử screenshot đã đối chiếu: Fork 3 chụp GDI sau khi ẩn overlay/chờ 80 ms; Fork 4–9 dùng snapshot GDI sau `AppWindow.MoveAndResize` nhưng trước `SetWindowPos(...TOPMOST)`. Fork 9 chỉ thêm `SelectedWindowScreenshotService` (WGC) ở nhánh **cửa sổ đã chọn**, không sửa Desktop. Thử đổi snapshot lên trước `MoveAndResize` trong Fork 10 tạo Desktop đen trên máy Ryzen9 nên đã hoàn nguyên chính xác thứ tự Fork 7/9. Log đúng là `[Screenshot] frozen-snapshot engine=gdi phase=fork7-compatible monitor=...`.
- `SelectedWindowScreenshotService.cs` phải luôn là nhánh độc lập: WGC/PrintWindow chỉ chạy lúc xuất một cửa sổ đã chọn, tuyệt đối không dùng cho snapshot Desktop, toàn màn hình hoặc vùng kéo. Nhờ vậy sửa Desktop không làm mất ảnh cửa sổ không bị che.
- Selector bỏ qua process `NVIDIA Overlay`/`NVIDIA Share` ở vùng taskbar/Desktop; không tắt NVIDIA Overlay hệ thống. Log verbose `[Screenshot] Ignored NVIDIA overlay window ...` chỉ để chẩn đoán khi selector gặp overlay đó.
- **Nghiệm thu Desktop/toàn màn hình (30/08/2026):** NVIDIA `CEF-OSC-WIDGET` và `TextInputHost.exe` (`Windows.UI.Core.CoreWindow`) đều là lớp phủ monitor; nếu selector nhận chúng thì click taskbar/khoảng trống đi sai nhánh WGC và có thể ra ảnh đen. Đã bỏ qua thêm TextInputHost/InputApp. Self-test bằng hotkey thật trên monitor 2560×1440: ảnh frozen GDI có nội dung Desktop đầy đủ; click taskbar rồi Ctrl+C ghi `[Screenshot] Copied mode=monitor`; ảnh Clipboard đọc lại là 2560×1440 với các pixel mẫu có brightness 55.7–226, không đen. Đây là điều kiện nghiệm thu cho Desktop/toàn màn hình; không đổi engine GDI nữa nếu test này còn đạt.
- **Desktop không taskbar (30/08/2026):** không suy luận Desktop chỉ bằng cửa sổ nằm dưới con trỏ. Một cửa sổ Settings/overlay có thể phủ phần nền trống, khiến selector cũ khóa nhầm riêng cửa sổ đó. `ScreenshotCaptureWindow` vì vậy có nút **Desktop (D)**: lấy `MONITORINFO.rcWork` cho đúng monitor, crop snapshot GDI theo work area và ghi `Copied mode=desktop`. Nút **Toàn màn hình (F)** dùng nguyên `rcMonitor`, gồm taskbar. Tự kiểm tra bằng UI Automation: chọn Desktop → Sao chép cho Clipboard **2560×1392** trên monitor 2560×1440 (đã bỏ 48 px taskbar), pixel mẫu không đen. Không lọc toàn bộ Settings khỏi bộ chọn, vì người dùng vẫn cần chụp chính cửa sổ Settings khi chủ động bấm nó.
- **Lỗi Debug không hiện giao diện (30/08/2026):** log cho thấy app dừng trong `CreateTrayMenuPresenterStyle` vì `Resources[typeof(MenuFlyoutPresenter)]` có thể chưa tồn tại ở direct/unpackaged Debug launch. Hàm nay trả `Style?`: nếu resource có thì áp style menu như cũ; nếu thiếu thì không gán custom style, để WinUI dùng native default tray style và log `[Tray] MenuFlyoutPresenter resource unavailable...`. Không được để style tuỳ biến làm `OnLaunched` thất bại. Đã smoke-test executable Debug chuẩn với `DESKBOX_DEV_DATA_ROOT` riêng: `OnLaunched completed successfully`, tray/hotkey đăng ký xong; sau đó test x64 **1790/1790 passed**.
- Thêm x86 cho `DeskBox`, `DeskBox.Updater`, project test, build/publish và installer. Installer `DeskBox.x86.iss` dùng chung detection/migration với x64 nhưng cài .NET 10 Runtime x86, Windows App Runtime x86 và VC++ x86; preflight vẫn chặn Windows dưới build 19044. Không hỗ trợ Windows 7/8.1.
- Manifest mới thêm `installers.{x86,x64,arm64}` (URL/SHA/size từng kiến trúc), vẫn ghi các trường primary x64 và ARM64 cũ nên Fork trước không mất update. Updater chọn theo `RuntimeInformation.ProcessArchitecture`; đồng thời từ chối update với thông báo rõ nếu Windows thấp hơn `minimumWindowsBuild`.
- Chính sách x86 giảm cache icon/bitmap/thumbnail và số tác vụ nạp song song; disk thumbnail cache x86 giới hạn 48 MB/750 file. Đây là giới hạn an toàn cho virtual address space 32-bit, không tắt thumbnail.
- Đã test x64: **1790/1790 passed**. Publish/installer x86/x64/ARM64 đã biên dịch local và manifest thử có đủ ba asset. Testhost x86 không chạy tại máy dev vì thiếu .NET 10 Runtime x86 (máy chỉ có .NET 8 x86); đây là thiếu dependency môi trường, không phải lỗi build. Trước khi phát hành, phải chạy installer trên Windows 10 x86/VM thật và test update từ một Fork x86 cũ sau khi có bản đó.

### Kiểm thử và môi trường

- Luôn test `x64`; không dùng `AnyCPU`. Lần kiểm thử toàn bộ gần nhất: 1790/1790 passed. Với x86, restore/publish/installer phải dùng `Platform=x86` và `RuntimeIdentifier=win-x86`; testhost cần .NET 10 Runtime x86 trên máy test.
- Khi DeskBox cài đặt đang chạy, đặt đúng biến `DESKBOX_DEV_DATA_ROOT` (không phải `DESKBOX_DATA_ROOT`) tới thư mục test tạm trước khi chạy Debug để recovery journal không khóa hay dùng `%LOCALAPPDATA%\DeskBox\data`. Có thể đặt thêm `DESKBOX_LOG_ROOT` nếu cần tách log. Xác minh log đầu phiên nằm dưới thư mục test trước khi thao tác Desktop.
- Nếu terminal gán `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` hoặc biến `GIT_*_PROXY` tới `127.0.0.1:9`, Git/Git Credential Manager sẽ không push được. Đây là lỗi môi trường terminal, không đổi remote fork và không lưu token vào Git config.

- After changing application code, first stop any running `DeskBox.exe` whose executable path is under this repository, then build the affected project, and start a fresh instance from the current Debug build unless the user explicitly asks not to restart it. Stopping before the build avoids locking the output executable.
- Với build có `RuntimeIdentifier`, executable Debug chuẩn là `src/DeskBox/bin/x64/Debug/net10.0-windows10.0.22621.0/win-x64/DeskBox.exe` (tương tự `win-x86`/`win-arm64`). Không chạy file `DeskBox.exe` ở tầng `net10.0-windows10.0.22621.0` vì đó có thể là output cũ không chứa thay đổi mới.
- After starting DeskBox, verify that exactly the intended repository build is running and report the executable path.
- Do not launch DeskBox from `Output`, `artifacts`, `.artifacts`, or `src/DeskBox/AppPackages` unless the user explicitly requests testing a packaged or published build.
- Preserve unrelated user changes and release artifacts. Ask before deleting material output directories or installer packages unless the user explicitly authorizes their removal.
- DeskBox is a packaged Windows application. Do not first run its tests with the default `AnyCPU` platform: MSIX packaging rejects a processor-neutral app-host executable. Run the test suite directly with `dotnet test .\tests\DeskBox.Tests\DeskBox.Tests.csproj --no-restore --verbosity:minimal -p:Platform=x64` (add `-p:RuntimeIdentifier=win-x64` when using architecture-specific restored assets).
- For Release publishing, always specify a matching platform and runtime identifier from the start: `-p:Platform=x86 -p:RuntimeIdentifier=win-x86` for x86, `-p:Platform=x64 -p:RuntimeIdentifier=win-x64` for x64, or `-p:Platform=ARM64 -p:RuntimeIdentifier=win-arm64` for ARM64. Keep `SelfContained=false` and `WindowsAppSDKSelfContained=false` for the runtime-download installer workflow unless the user requests a self-contained build.
- The explicit architecture rules above apply to tests and Release publishing. Continue using the canonical non-platform Debug output for the normal local restart workflow.
